name: Regression

on:
  workflow_dispatch:
  push:
    branches:
    - main
  pull_request:

jobs:
  regression:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v2

      #- name: make arm containers work
        #run: |
          #sudo apt-get update
          #sudo apt-get install qemu-system-arm qemu-efi qemu-user-static -y

      - name: build restapi
        run: |
          cd restapi
          ./key.sh make

      - name: regression test restapi
        run: |
          cd restapi
          ./key.sh regression



      - name: build website
        run: |
          cd website
          ./key.sh make

      - name: regression test website
        run: |
          cd website
          ./key.sh regression
